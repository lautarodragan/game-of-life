export namespace RenderModes {
  export enum CellShape {
    Squares, Circles, Lines1, Lines2
  }
  export enum Clear {
    PlainWhite, PlainBlack, FadeWhite, FadeBlack, Random
  }
  export enum CellColor {
    Black, White, Random
  }
}